version: "3"
services:
  shitcoin-factory:
    build: ./
    image: shitcoin-factory
    working_dir: /usr/src/app
    volumes:
      - ./:/usr/src/app
      - exclude:/usr/src/app/node_modules/
    ports:
      - 8545:8545

volumes:
  exclude:
